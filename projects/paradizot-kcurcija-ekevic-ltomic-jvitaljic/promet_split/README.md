Promet Split — Frontend demo

This folder contains a React frontend demo for a local bus network app.

How to run

1. cd promet_split
2. npm install
3. npm run dev

Notes
- The app is frontend-only; actions such as "Pay" are mocked.
- Map is an interactive mock with clickable stops (no external map provider used).
